#include<stdio.h>
#include<math.h>
int main()
{
    float a,b,c,x,y,z,A;
    printf("Enter the value of a: ");
    scanf("%f",&a);
    y=pow(a,5);
    printf("Enter the value of b: ");
    scanf("%f",&b);
    y=pow(b,5);
    printf("Enter the value of c: ");
    scanf("%f",&c);
    z=pow(c,5);
    A=(x+y+z);
    printf ("Enter the result of A is: %.3f",A);
    return 0;
}
